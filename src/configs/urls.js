const jsonBaseURL = "https://jsonplaceholder.typicode.com";
const spaceXBaseURL = "https://api.spacexdata.com/v3";

const urls = {
    posts: "/posts",
    launches: "/launches"
}

export {urls, jsonBaseURL, spaceXBaseURL}